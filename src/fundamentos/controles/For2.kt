package fundamentos.controles

//for

fun main(args: Array<String>) {
    for (i in 10 downTo 1) {          // downTo - é parecido com .. - range, sendo que pega só do maior para o menor
        println("i = $i")
    }
 //Se eu colocar do menor para o maior, não faz nada, porque não funciona.
}